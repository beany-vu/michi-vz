import { Meta } from "@storybook/blocks";

import Github from "./assets/github.svg";
import Discord from "./assets/discord.svg";
import Youtube from "./assets/youtube.svg";
import Tutorials from "./assets/tutorials.svg";
import Styling from "./assets/styling.png";
import Context from "./assets/context.png";
import Assets from "./assets/assets.png";
import Docs from "./assets/docs.png";
import Share from "./assets/share.png";
import FigmaPlugin from "./assets/figma-plugin.png";
import Testing from "./assets/testing.png";
import Accessibility from "./assets/accessibility.png";
import Theming from "./assets/theming.png";
import AddonLibrary from "./assets/addon-library.png";

export const RightArrow = () => <svg 
    viewBox="0 0 14 14" 
    width="8px" 
    height="14px" 
    style={{ 
      marginLeft: '4px',
      display: 'inline-block',
      shapeRendering: 'inherit',
      verticalAlign: 'middle',
      fill: 'currentColor',
      'path fill': 'currentColor'
    }}
>
  <path d="m11.1 7.35-5.5 5.5a.5.5 0 0 1-.7-.7L10.04 7 4.9 1.85a.5.5 0 1 1 .7-.7l5.5 5.5c.2.2.2.5 0 .7Z" />
</svg>

<Meta title="Configure your project" />

<div className="sb-container">
  <div className='sb-section-title'>
    # Configure Michi-VZ Project

    Welcome to Michi-VZ, a visualization library based on D3 and React. This guide will help you set up and configure your project to work with this library.
  </div>
  
  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">Installation</h4>
      <p className="sb-section-item-paragraph">
        First, install the package and its peer dependencies:
        
        ```bash
        npm install michi-vz
        # Install peer dependencies if needed
        npm install react react-dom d3 styled-components
        ```
      </p>
    </div>

    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">TypeScript Configuration</h4>
      <p className="sb-section-item-paragraph">
        If you're using TypeScript, ensure your `tsconfig.json` includes these settings:
        
        ```json
        {
          "compilerOptions": {
            "jsx": "react-jsx",
            "esModuleInterop": true,
            "skipLibCheck": true,
            "moduleResolution": "node"
          }
        }
        ```
      </p>
    </div>

    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">Provider Setup</h4>
      <p className="sb-section-item-paragraph">
        Wrap your application or component with the `MichiVzProvider` to provide theme context:
        
        ```jsx
        import { MichiVzProvider } from "michi-vz";
        
        const App = () => (
          <MichiVzProvider
            colorsMapping={{
              Exports: "green",
              Imports: "red",
              Africa: "orange",
              "Non-LDC": "purple",
              Sudan: "blue",
            }}
            // Optional: disabledItems={["Non-LDC"]}
          >
            <YourComponent />
          </MichiVzProvider>
        );
        ```
      </p>
    </div>
  </div>
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    # Using Chart Components
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">Basic Usage</h4>
      <p className="sb-section-item-paragraph">
        Import and use any chart component with appropriate props:
        
        ```jsx
        import { VerticalStackBarChart } from "michi-vz";
        
        const MyChart = () => (
          <VerticalStackBarChart
            dataSet={[
              {
                seriesKey: "Africa",
                seriesKeyAbbreviation: "Africa",
                series: [
                  { date: "2001", Africa: "666" },
                  { date: "2002", Africa: "777" },
                  { date: "2003", Africa: "989" },
                ],
              },
              // Add more series as needed
            ]}
            width={900}
            height={480}
            margin={{ top: 50, right: 50, bottom: 50, left: 50 }}
            title="My Chart Title"
          />
        );
        ```
      </p>
    </div>

    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">Filtering and Sorting</h4>
      <p className="sb-section-item-paragraph">
        Many charts support filtering and sorting options:
        
        ```jsx
        <VerticalStackBarChart
          // ...other props
          filter={{
            limit: 3,           // Show top 3 items
            sortingDir: "desc", // Sort descending
            date: "2002",       // Filter by this date
          }}
        />
        ```
      </p>
    </div>

    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">Advanced Customization</h4>
      <p className="sb-section-item-paragraph">
        Customize with callback functions and formatting options:
        
        ```jsx
        <VerticalStackBarChart
          // ...other props
          yAxisFormat={d => `$${d}`}
          onChartDataProcessed={metadata => {
            console.log(metadata);
            // Use chart metadata in your app
          }}
        />
        ```
      </p>
    </div>
  </div>
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    # Development and Contributing
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">Local Development</h4>
      <p className="sb-section-item-paragraph">
        1. Clone the repository
        2. Install dependencies: `npm install`
        3. Run Storybook: `npm run storybook`
        4. Visit http://localhost:6006 to view components
      </p>
    </div>

    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">Linting and Formatting</h4>
      <p className="sb-section-item-paragraph">
        - Run `npm run lint` to check for linting errors
        - Run `npm run lint:fix` to automatically fix linting issues
        - We follow the project's ESLint and Prettier configurations
      </p>
    </div>

    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">Building and Publishing</h4>
      <p className="sb-section-item-paragraph">
        1. Update version in package.json
        2. Run `npm run prepublishOnly` to build the package
        3. Use `npm publish` to publish to npm
      </p>
    </div>
  </div>
</div>

<div className="sb-container">
  <div className='sb-section-title'>
    # Testing
  </div>

  <div className="sb-section">
    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">Running Tests</h4>
      <p className="sb-section-item-paragraph">
        We use Jest and React Testing Library for unit tests. Run tests with:
        
        ```bash
        # Run all tests
        npm run test
        
        # Run tests in watch mode
        npm run test:watch
        
        # Update snapshots
        npm run test -- -u
        
        # Generate coverage report
        npm run test -- --coverage
        ```
      </p>
    </div>

    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">Test Structure</h4>
      <p className="sb-section-item-paragraph">
        Tests are located in `src/components/__tests__/`. Each component has a corresponding test file:
        
        ```
        src/
          components/
            __tests__/
              MichiVzProvider.test.tsx
              VerticalStackBarChart.test.tsx
              LineChart.test.tsx
              test-utils.tsx  # Common utilities
        ```
      </p>
    </div>

    <div className="sb-section-item">
      <h4 className="sb-section-item-heading">Writing Tests</h4>
      <p className="sb-section-item-paragraph">
        Use the `customRender` function from test-utils to render components with proper context:
        
        ```jsx
        import { customRender } from "./test-utils";
        
        test("renders component", () => {
          const { container } = customRender(
            <YourComponent {...props} />
          );
          
          // Assert things about your component
        });
        ```
        
        SVG and D3 functions are automatically mocked for testing.
      </p>
    </div>
  </div>
</div>

<div className='sb-addon'>
  <div className='sb-addon-text'>
    <h4>Addons</h4>
    <p className="sb-section-item-paragraph">Integrate your tools with Storybook to connect workflows.</p>
    <a
        href="https://storybook.js.org/integrations/"
        target="_blank"
      >Discover all addons<RightArrow /></a>
  </div>
  <div className='sb-addon-img'>
    <img src={AddonLibrary} alt="Integrate your tools with Storybook to connect workflows." />
  </div>
</div>

<div className="sb-section sb-socials">
    <div className="sb-section-item">
      <img src={Github} alt="Github logo" className="sb-explore-image"/>
      Join our contributors building the future of UI development.

      <a
        href="https://github.com/storybookjs/storybook"
        target="_blank"
      >Star on GitHub<RightArrow /></a>
    </div>
    <div className="sb-section-item">
      <img src={Discord} alt="Discord logo" className="sb-explore-image"/>
      <div>
        Get support and chat with frontend developers.

        <a
          href="https://discord.gg/storybook"
          target="_blank"
        >Join Discord server<RightArrow /></a>
      </div>
    </div>
    <div className="sb-section-item">
      <img src={Youtube} alt="Youtube logo" className="sb-explore-image"/>
      <div>
        Watch tutorials, feature previews and interviews.

        <a
          href="https://www.youtube.com/@chromaticui"
          target="_blank"
        >Watch on YouTube<RightArrow /></a>
      </div>
    </div>
    <div className="sb-section-item">
      <img src={Tutorials} alt="A book" className="sb-explore-image"/>
      <p>Follow guided walkthroughs on for key workflows.</p>

      <a
          href="https://storybook.js.org/tutorials/"
          target="_blank"
        >Discover tutorials<RightArrow /></a>
    </div>
</div>

<style>
  {`
  .sb-container {
    margin-bottom: 48px;
  }

  .sb-section {
    width: 100%;
    display: flex;
    flex-direction: row;
    gap: 20px;
  }

  img {
    max-height: 90px;
    max-width: 200px;
    margin-bottom: 12px;
  }

  .sb-section-title {
    margin-bottom: 32px;
  }

  .sb-section-item, .sb-grid-item {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .sb-section-item-heading {
    padding-top: 20px !important;
    padding-bottom: 5px !important;
    margin: 0 !important;
  }
  
  .sb-section-item-paragraph {
    margin: 0;
    padding-bottom: 10px;
  }

  .sb-chevron {
    margin-left: 5px;
  }

  .sb-features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    grid-gap: 32px 20px;
  }

  .sb-socials {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
  }

  .sb-socials p {
    margin-bottom: 10px;
  }

  .sb-explore-image {
    max-height: 32px;
    align-self: flex-start;
  }

  .sb-addon {
    width: 100%;
    display: flex;
    align-items: center;
    position: relative;
    background-color: #EEF3F8;
    border-radius: 5px;
    border: 1px solid rgba(0, 0, 0, 0.05);
    padding: 20px;
    margin-bottom: 40px;
    gap: 20px;
  }

  .sb-addon-text {
    color: #333333;
    font-size: 14px;
  }

  .sb-addon-text h4 {
    padding-top: 0px !important;
  }

  .sb-addon-img {
    height: 160px;
    width: auto;
    filter: drop-shadow(0px 4px 10px #00000020);
  }
  
  pre {
    background-color: #f8f8f8;
    padding: 12px;
    border-radius: 4px;
    overflow-x: auto;
  }
  
  code {
    font-family: monospace;
  }

  @media screen and (max-width: 800px) {
    .sb-section {
      flex-direction: column;
    }

    .sb-features-grid {
      grid-template-columns: repeat(1, 1fr);
    }

    .sb-socials {
      grid-template-columns: repeat(2, 1fr);
    }

    .sb-addon {
      flex-direction: column;
      align-items: flex-start;
    }
  }
  `}
</style>
